# Note: torch, torchvision, torchaudio are pre-installed in AWS DLC PyTorch 2.1.2
# and should not be reinstalled to avoid conflicts and reduce build time

# Note: The following packages are pre-installed in AWS DLC PyTorch 2.1.2 SageMaker variant:
# - scikit-learn, pandas, numpy
# - pyarrow 17.0.0 (DO NOT downgrade - causes ABI conflicts and memory corruption)
# - pydantic 2.8.2 (DO NOT downgrade - may cause dependency conflicts)
# - matplotlib 3.9.1 (DO NOT downgrade - may cause numpy ABI issues)

# IMPORTANT: PyTorch 2.1.0 inference containers do NOT include pydantic pre-installed
# Must explicitly install for inference compatibility
pydantic>=2.0.0

# Transformers & HuggingFace
transformers==4.37.2

# Lightning
lightning==2.1.3
lightning-utilities==0.10.1

# Metrics
torchmetrics==1.7.1

# TensorBoard (for logging and visualization)
tensorboard==2.16.2

# Data Processing
beautifulsoup4==4.12.3
gensim==4.3.1

# ONNX Export
onnx==1.15.0
onnxruntime==1.17.0

# Serving
flask==3.0.2
