# Core AWS and SageMaker dependencies
boto3>=1.39.0
botocore>=1.39.0
sagemaker>=2.248.0

# Data validation and configuration
pydantic>=2.11.0
PyYAML>=6.0.0

# Graph and workflow management
networkx>=3.5.0

# CLI and utilities
click>=8.2.0
requests>=2.32.0
packaging>=24.2.0
typing_extensions>=4.14.0

# Data processing (required by scripts)
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.3.0
joblib>=1.5.0
pyarrow>=14.0.0

# Optional ML frameworks (used in specific scripts)
xgboost>=2.0.0
matplotlib>=3.8.0

# Note: Jupyter dependencies moved to optional dependencies in pyproject.toml
# Install with: pip install cursus[jupyter] if needed
